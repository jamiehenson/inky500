---
import { cn } from "@/lib/utils";

const { className, ...props } = Astro.props;
---

<div
  class={cn(
    "relative rounded-lg border bg-card text-card-foreground shadow-sm h-full",
    className
  )}
  {...props}
>
  <slot />
</div>
