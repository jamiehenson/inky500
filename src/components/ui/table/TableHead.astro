---
import { cn } from "@/lib/utils";

const { className, ...props } = Astro.props;
---

<th
  class={cn(
    "h-10 px-3 text-left align-middle font-bold text-muted-foreground [&:has([role=checkbox])]:pr-0 [&>[role=checkbox]]:translate-y-[2px]",
    className
  )}
  {...props}
>
  <slot />
</th>
